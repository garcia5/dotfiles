require('gitsigns').setup({
    numhl              = false, -- highlight changed lines
    current_line_blame = true, -- show commit info on current line
    update_debounce    = 500,  -- update every 500ms
})
